[
  {
    "protocol_id": "xfs.transaction.alloc.cancel_or_commit",
    "resource_kind": "xfs_transaction",
    "acquire_functions": ["xfs_trans_alloc", "xfs_trans_alloc_empty"],
    "release_functions": ["xfs_trans_cancel", "xfs_trans_commit"],
    "success_condition": "return_value == 0 and out transaction pointer assigned",
    "resource_expr": "out_parameter",
    "required_action": "xfs_trans_cancel",
    "exceptions": ["transaction_committed", "ownership_transferred", "released_by_wrapper", "resource_not_successfully_acquired"],
    "evidence_type": "api_lifecycle",
    "evidence_level": "E2_API_PROTOCOL",
    "confidence": "high",
    "description": "Successful XFS transaction allocation must end with xfs_trans_cancel on error paths or xfs_trans_commit on commit paths."
  }
]
