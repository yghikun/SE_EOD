[
  {
    "protocol_id": "xfs.inode.ilock.iunlock",
    "resource_kind": "xfs_inode_lock",
    "acquire_functions": ["xfs_ilock"],
    "release_functions": ["xfs_iunlock"],
    "success_condition": "lock acquired after call returns",
    "resource_expr": "argument0",
    "required_action": "xfs_iunlock",
    "exceptions": ["ownership_transferred", "released_by_wrapper", "lock_released_by_callee"],
    "evidence_type": "api_lifecycle",
    "evidence_level": "E2_API_PROTOCOL",
    "confidence": "high",
    "description": "XFS inode locks acquired by xfs_ilock must be released by xfs_iunlock."
  },
  {
    "protocol_id": "xfs.buffer.lock.unlock",
    "resource_kind": "xfs_buf_lock",
    "acquire_functions": ["xfs_buf_lock"],
    "release_functions": ["xfs_buf_unlock", "xfs_buf_relse"],
    "success_condition": "lock acquired after call returns",
    "resource_expr": "argument0",
    "required_action": "xfs_buf_unlock",
    "exceptions": ["ownership_transferred", "released_by_wrapper", "released_by_xfs_buf_relse"],
    "evidence_type": "api_lifecycle",
    "evidence_level": "E2_API_PROTOCOL",
    "confidence": "high",
    "description": "XFS buffer locks acquired by xfs_buf_lock are released by xfs_buf_unlock or by xfs_buf_relse, which unlocks before dropping the buffer reference."
  },
  {
    "protocol_id": "xfs.dquot.lock.unlock",
    "resource_kind": "xfs_dquot_lock",
    "acquire_functions": ["xfs_dqlock"],
    "release_functions": ["xfs_dqunlock"],
    "success_condition": "lock acquired after call returns",
    "resource_expr": "argument0",
    "required_action": "xfs_dqunlock",
    "exceptions": ["ownership_transferred", "released_by_wrapper", "released_by_xfs_qm_dqput"],
    "evidence_type": "api_lifecycle",
    "evidence_level": "E2_API_PROTOCOL",
    "confidence": "high",
    "description": "XFS dquot locks acquired by xfs_dqlock must be released by xfs_dqunlock or through dqput paths."
  },
  {
    "protocol_id": "lock.generic.mutex.spin.rwsem",
    "resource_kind": "mutex",
    "acquire_functions": ["mutex_lock"],
    "release_functions": ["mutex_unlock"],
    "success_condition": "lock acquired after call returns",
    "resource_expr": "argument0",
    "required_action": "mutex_unlock",
    "exceptions": ["ownership_transferred", "released_by_wrapper"],
    "evidence_type": "api_lifecycle",
    "evidence_level": "E2_API_PROTOCOL",
    "confidence": "high",
    "description": "Generic mutex acquisitions must be released on exits."
  },
  {
    "protocol_id": "lock.generic.spin.variants",
    "resource_kind": "spinlock",
    "acquire_functions": ["spin_lock", "spin_lock_irq", "spin_lock_irqsave"],
    "release_functions": ["spin_unlock", "spin_unlock_irq", "spin_unlock_irqrestore"],
    "success_condition": "lock acquired after call returns",
    "resource_expr": "argument0",
    "required_action": "spin_unlock",
    "exceptions": ["ownership_transferred", "released_by_wrapper"],
    "evidence_type": "api_lifecycle",
    "evidence_level": "E2_API_PROTOCOL",
    "confidence": "high",
    "description": "Generic spin lock variants must use a matching unlock variant."
  },
  {
    "protocol_id": "lock.generic.rwsem",
    "resource_kind": "rwsem",
    "acquire_functions": ["down_read", "down_write"],
    "release_functions": ["up_read", "up_write"],
    "success_condition": "rwsem acquired after call returns",
    "resource_expr": "argument0",
    "required_action": "up_read",
    "exceptions": ["ownership_transferred", "released_by_wrapper"],
    "evidence_type": "api_lifecycle",
    "evidence_level": "E2_API_PROTOCOL",
    "confidence": "high",
    "description": "rwsem acquisitions must be released by the corresponding up_* helper."
  }
]
