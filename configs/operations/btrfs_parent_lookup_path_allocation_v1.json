{
  "operation_schema_version": 1,
  "operation_id": "btrfs_parent_lookup_path_allocation",
  "family_id": "allocation_lifecycle",
  "binding_id": "btrfs.path_allocation",
  "entry_functions": ["btrfs_get_parent"],
  "phases": ["ENTRY", "SUCCESS", "FAILURE"],
  "role_instances": [
    {"abstract_role": "resource", "selector": "summary-bound"}
  ],
  "discovery": {
    "required_callees": ["btrfs_alloc_path", "btrfs_free_path"],
    "semantic_patterns": ["mutation_failure_cleanup"]
  },
  "return_contracts": [
    {
      "contract_id": "btrfs.parent_lookup.failure",
      "guard": "IS_ERR(ret)",
      "outcome": "failure",
      "priority": 20
    },
    {
      "contract_id": "btrfs.parent_lookup.success",
      "guard": "!IS_ERR(ret)",
      "outcome": "success",
      "priority": 10
    }
  ],
  "effect": {
    "event_id": "btrfs.search_path.allocated",
    "effect_id": "btrfs.search_path.allocation",
    "abstract_role": "resource",
    "kind": "METADATA_UPDATE",
    "scope": "LOCAL",
    "owner": "btrfs_path_allocator",
    "phase": "ENTRY",
    "required": true,
    "description": "A non-null Btrfs search path must be released on every reachable operation exit."
  },
  "summary_ids": {
    "acquire": "btrfs.search_path.alloc",
    "release": "btrfs.search_path.free"
  },
  "legal_exits": [
    {
      "exit_id": "btrfs.parent_lookup.success",
      "kind": "success",
      "phases": ["SUCCESS"],
      "completion_modes": ["COMMITTED", "ROLLED_BACK"],
      "return_outcomes": ["success"]
    },
    {
      "exit_id": "btrfs.parent_lookup.failure",
      "kind": "failure",
      "phases": ["FAILURE"],
      "completion_modes": ["ROLLED_BACK"],
      "return_outcomes": ["failure"]
    }
  ],
  "description": "Development operation instance for explicit Btrfs search path allocation and release."
}
