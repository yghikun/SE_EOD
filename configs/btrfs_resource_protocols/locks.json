[
  {
    "protocol_id": "lock.mutex.lock.unlock",
    "resource_kind": "mutex",
    "acquire_functions": ["mutex_lock"],
    "release_functions": ["mutex_unlock"],
    "success_condition": "lock acquired after call returns",
    "resource_expr": "argument0",
    "required_action": "mutex_unlock",
    "exceptions": ["ownership_transferred", "released_by_wrapper"],
    "evidence_type": "api_lifecycle",
    "evidence_level": "E2_API_PROTOCOL",
    "confidence": "high",
    "description": "Mutex acquisitions must be unlocked on exits."
  },
  {
    "protocol_id": "lock.spin.variants",
    "resource_kind": "spinlock",
    "acquire_functions": ["spin_lock", "spin_lock_irq", "spin_lock_irqsave"],
    "release_functions": ["spin_unlock", "spin_unlock_irq", "spin_unlock_irqrestore"],
    "success_condition": "lock acquired after call returns",
    "resource_expr": "argument0",
    "required_action": "spin_unlock",
    "exceptions": ["ownership_transferred", "released_by_wrapper"],
    "evidence_type": "api_lifecycle",
    "evidence_level": "E2_API_PROTOCOL",
    "confidence": "high",
    "description": "Spin lock variants must use the corresponding unlock variant."
  },
  {
    "protocol_id": "lock.rwlock.read",
    "resource_kind": "rwlock_read",
    "acquire_functions": ["read_lock"],
    "release_functions": ["read_unlock"],
    "success_condition": "read lock acquired after call returns",
    "resource_expr": "argument0",
    "required_action": "read_unlock",
    "exceptions": ["ownership_transferred", "released_by_wrapper"],
    "evidence_type": "api_lifecycle",
    "evidence_level": "E2_API_PROTOCOL",
    "confidence": "high",
    "description": "Read locks must be released with read_unlock."
  },
  {
    "protocol_id": "lock.rwlock.write",
    "resource_kind": "rwlock_write",
    "acquire_functions": ["write_lock"],
    "release_functions": ["write_unlock"],
    "success_condition": "write lock acquired after call returns",
    "resource_expr": "argument0",
    "required_action": "write_unlock",
    "exceptions": ["ownership_transferred", "released_by_wrapper"],
    "evidence_type": "api_lifecycle",
    "evidence_level": "E2_API_PROTOCOL",
    "confidence": "high",
    "description": "Write locks must be released with write_unlock."
  },
  {
    "protocol_id": "lock.btrfs.tree.write",
    "resource_kind": "btrfs_tree_lock",
    "acquire_functions": ["btrfs_tree_lock"],
    "release_functions": ["btrfs_tree_unlock"],
    "success_condition": "tree lock acquired after call returns",
    "resource_expr": "argument0",
    "required_action": "btrfs_tree_unlock",
    "exceptions": ["ownership_transferred", "released_by_wrapper", "released_by_btrfs_path"],
    "evidence_type": "api_lifecycle",
    "evidence_level": "E2_API_PROTOCOL",
    "confidence": "high",
    "description": "Btrfs extent-buffer write locks must be released explicitly or through path cleanup."
  },
  {
    "protocol_id": "lock.btrfs.tree.read",
    "resource_kind": "btrfs_tree_read_lock",
    "acquire_functions": ["btrfs_tree_read_lock"],
    "release_functions": ["btrfs_tree_read_unlock"],
    "success_condition": "tree read lock acquired after call returns",
    "resource_expr": "argument0",
    "required_action": "btrfs_tree_read_unlock",
    "exceptions": ["ownership_transferred", "released_by_wrapper", "released_by_btrfs_path"],
    "evidence_type": "api_lifecycle",
    "evidence_level": "E2_API_PROTOCOL",
    "confidence": "high",
    "description": "Btrfs extent-buffer read locks must be released explicitly or through path cleanup."
  }
]
