# P0.4 Traceability Matrix

Status values: `READY` rules have independent design/normal-source evidence,
a confirmed counterexample, a repair or safe sibling, and a negative boundary.
No `BUG_DERIVED_ONLY` rule is eligible for the frozen catalog.

| Protocol | Rule | Document/design evidence | Normal source | Bug path | Fixed path | Safe negative | Status |
|---|---|---|---|---|---|---|---|
| `MetadataTransitionOutcome` | MTO-R1: success at settlement requires completed transition | Callers branch on helper error; binary return convention distinguishes completed success from failure | Linux v7.1 `ext4_fc_replay_inode()` returns `ret`; normal replay completes before success | #5; validation #1/#2 | v7.1 ext4 function diff | Resource cleanup with no metadata transition is out of footprint | READY |
| `MetadataTransitionOutcome` | MTO-R2: completed transition with no active failure must not report error | Retry semantics replace a recoverable attempt; stale result provenance is not current operation outcome | Successful extra-isize fallback updates inode then should return success | #4 | Submitted stale-error fix described in corpus | A completed transition may report error only when the entry contract explicitly allows completed-with-error; that contract is absent here | READY |
| `MetadataTransitionOutcome` | MTO-O1: observed failure must be propagated or superseded by proven completion | XFS growfs cancels its transaction on helper error; ext4 replay driver checks helper returns | Error-return siblings reach caller cancellation/abort | #5; held-out #8/#13 | v7.1 ext4 and XFS function diffs | Unknown helper completion yields `INCOMPLETE`, not success | READY |
| `MetadataTransitionOutcome` | MTO-D1: check at `OperationReturn` | Helper return is the point at which caller chooses continue/cancel | Normal callers consume return immediately | #4/#5 | fixed functions return current result | Internal cleanup before return does not itself settle outcome | READY |
| `FailureRollbackConformance` | FRC-I1: active/attached targets must remain live and role-valid at exposure | Btrfs assertions and pointer consumers require live devices/roots | Normal sprout/recovery paths retain live members or restore prior targets | #7/#17 | #7 repair direction and sprout patch 2/3 | Detached resource leak with no active relation is outside footprint | READY |
| `FailureRollbackConformance` | FRC-O1: failure after relation mutation requires prestate restoration or permitted delegation | Operation-local changes span fs roots, device lists, active pointers, membership, and fsid; abort is not specified to restore arbitrary in-memory relations | Abort/safe cleanup explicitly detaches or teardown drops guarded attachments | #7/#16/#18 | #7 accepted fix direction; sprout patch series | Failure before any protocol relation change activates no rollback obligation | READY |
| `FailureRollbackConformance` | FRC-O2: delegation is not discharge and must name allowed authority | QEMU safe path proves one guarded teardown owner; non-abort path proves the guard is not universal | `BTRFS_FS_ERROR` teardown owns/drops roots only on the guarded path | #7 non-abort path | #7 abort safe path | Unknown cleanup authority yields `INCOMPLETE` | READY |
| `FailureRollbackConformance` | FRC-D1: relation-specific deadline is earliest exposure, operation return, or owner termination | Stale active pointer can be consumed before return; update-list attachment must be removed before device free | Normal paths restore before pointer use/release | #16/#17/#18 | sprout fixed-run evidence | A relation fully isolated with closed escape proof may defer only to its declared later deadline | READY |

